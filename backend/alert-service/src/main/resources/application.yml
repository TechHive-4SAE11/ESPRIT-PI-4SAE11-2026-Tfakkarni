spring:
  application:
    name: alert-service

  # config service/client
  cloud:
    config:
      enabled: true
  config:
    import: optional:configserver:http://localhost:8888

# actuator
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
