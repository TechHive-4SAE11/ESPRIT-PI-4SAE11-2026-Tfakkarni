server:
  port: 18084

spring:
  datasource:
    url: jdbc:postgresql://ep-damp-bar-agi72bgl-pooler.c-2.eu-central-1.aws.neon.tech:5432/neondb?sslmode=require&channel_binding=require
    username: neondb_owner
    password: npg_8ZUiCsXMu0jd
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  # Console H2 pour voir les données (SUPER UTILE !)
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true

  # Redis configuration (gardé inchangé)
  data:
    redis:
      host: live-bullfrog-45869.upstash.io
      port: 6379
      password: AbMtAAIncDJjOWExMjVkOTcwYjk0MGM2OWY0MzZhNmUwMWYwNGQ0OXAyNDU4Njk
      ssl:
        enabled: true
      timeout: 60000
      lettuce:
        pool:
          max-active: 10
          max-idle: 5
          min-idle: 1

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry: true
    register-with-eureka: true

# Firebase configuration (gardé inchangé)
firebase:
  config-file: classpath:firebase-service-account.json

# Tracking service URL (gardé inchangé)
tracking-service:
  url: http://localhost:18083

# Notification scheduler (gardé inchangé)
notification:
  scheduler:
    enabled: true
    medication-reminder-cron: "0 */1 * * * *"