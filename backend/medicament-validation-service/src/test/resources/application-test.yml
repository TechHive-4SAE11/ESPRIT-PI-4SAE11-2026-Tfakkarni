spring:
  batch:
    job:
      enabled: false
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    username: sa
    password:

medicament:
  scheduler:
    enabled: false
    load-on-startup: false
    refresh-cron: "0 0 3 * * *"

openfda:
  api:
    base-url: http://localhost/openfda-test
    page-size: 1
    max-pages: 1

eureka:
  client:
    enabled: false
