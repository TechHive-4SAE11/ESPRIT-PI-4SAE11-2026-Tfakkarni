spring:
  batch:
    job:
      enabled: false
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    username: sa
    password:

medicament:
  scheduler:
    enabled: false
    load-on-startup: false

eureka:
  client:
    enabled: false
