# ============================================================
# Tfakkarni — Secret (sensitive values — base64 encoded)
# Replace placeholder values with actual base64-encoded secrets
# echo -n "value" | base64
# ============================================================
apiVersion: v1
kind: Secret
metadata:
  name: tfakkarni-secrets
  namespace: tfakkarni
type: Opaque
data:
  # Replace these with your actual base64-encoded values
  NEON_GAME_DB_URL: REPLACE_WITH_BASE64
  NEON_GAME_DB_USERNAME: REPLACE_WITH_BASE64
  NEON_GAME_DB_PASSWORD: REPLACE_WITH_BASE64
